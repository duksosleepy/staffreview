# Server
PORT=3001
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Frontend URL (for redirect after auth)
FRONTEND_URL=http://localhost:5173

# OIDC Provider (Casdoor) - CRITICAL: Use HTTPS in production
OIDC_ENDPOINT=https://your-casdoor-server.com
OIDC_CLIENT_ID=your-client-id
OIDC_CLIENT_SECRET=your-client-secret  # CRITICAL: Store securely, never commit to version control
OIDC_REDIRECT_URI=http://localhost:3001/auth/callback
OIDC_SCOPE=openid profile email

# JWT Secret - CRITICAL: Use strong random secret, min 32 characters
# Generate with: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string-min-32-chars

# Security Headers
SECURE_COOKIES=true  # Set to true in production with HTTPS
