{
  "name": "staffreview",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome lint --write frontend/src server/src",
    "format": "biome format --write frontend/src server/src",
    "check": "biome check --write frontend/src server/src"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11"
  }
}
